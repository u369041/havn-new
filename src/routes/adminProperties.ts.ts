// src/routes/adminProperties.ts
// ⚠️ LEGACY ALIAS FILE (do not add logic here)
//
// This project uses the canonical admin properties router in:
//   src/routes/admin-properties.ts
//
// We keep this file ONLY to prevent accidental breakage if something imports
// "adminProperties" by old name. It must never define routes directly.

import router from "./admin-properties";

export default router;