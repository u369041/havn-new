{
  "name": "havn-new",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "postinstall": "prisma generate",
    "prestart": "node dist/migrate.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.18.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",

    /* Install types & tools as runtime deps so Render always has them */
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "prisma": "^5.18.0",
    "typescript": "^5.4.0"
  },
  "devDependencies": {}
}
